<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="integrapp.tta.intel.ehu.eus.integrapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.RECORD_VIDEO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vista.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".vista.AukerakActivity" />
        <activity android:name=".vista.JolasakActivity" />
        <activity android:name=".vista.AutoebaluazioaActivity" />
        <activity android:name=".vista.AudioActivity" />
        <activity android:name=".vista.AhorcadoActivity" />
        <activity android:name=".vista.MemoryActivity" />
        <activity android:name=".vista.UnirActivity" />
        <activity android:name=".vista.TestActivity" />
        <activity android:name=".vista.MapaActivity" />
        <activity android:name=".vista.Mapa2Activity"></activity>
    </application>

</manifest>